import { Routes } from '@angular/router';
import { AuthFirstLoginPasswordComponent } from 'app/modules/auth/first-login-password/first-login-password.component';

export default [
    {
        path: '',
        component: AuthFirstLoginPasswordComponent,
    },
] as Routes;
